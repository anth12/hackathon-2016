@html.extend('layout', function(model){

    @html.block('content', function(model){
            
        <h2>Error: @model.channel</h2>

        <p style="color: black">
            @model.message
        </p>

        <p style="color: black">
            @model.error
        </p>

    })

})