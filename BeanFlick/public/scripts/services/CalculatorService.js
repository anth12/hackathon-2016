App.service('CalculatorService', function () {
    
    this.movementData = function (down, up) {
        var dy = down.y - up.y;
        // Remember to flip coordinates because 0,0 is top left NOT bottom right
        var dx = up.x - down.x;
        

        return;
    }

})